import 'package:flutter/material.dart';

class CropperImageScreen extends StatelessWidget {
  const CropperImageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
